# Session signing key — generate with: openssl rand -hex 32
SESSION_SECRET=

# URLs — adjust per environment
FRONTEND_URL=http://localhost:5173

# Neon — https://console.neon.tech — used by CI for DB branch isolation
NEON_API_KEY=
NEON_PROJECT_ID=

# Vite frontend — optional, falls back to localhost:3000
VITE_API_URL=
VITE_WS_URL=
